# Frontend
## Installation des packages
Pour installer les packages et lancer le frontend :
1. `cd frontend`
2. `npm install`
3. `npm run dev`
4. Ouvrir la page http://localhost:5173

## Connection avec le frontend
1. Créer le fichier `frontend/.env.local`
2. Ajouter `VITE_API_URL=http://localhost:8080`